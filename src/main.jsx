import React from 'react'
import ReactDOM from 'react-dom/client'
import TalkToMyDB from './app.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <TalkToMyDB />
  </React.StrictMode>,
)
